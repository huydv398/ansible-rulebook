---
- name: EDA - Add info VM to netbox
  hosts: localhost
  sources:
    - name: listen webhook
      ansible.eda.webhook:
        host: 0.0.0.0
        port: 5000

  rules: 
    - name: Run Job template Add agent
      condition: event.payload.event.message is match ("*deoloying*")
      action:
        run_workflow_template:
          name: 2.add_vm_to_netbox
          organization: Default
          retries: 3
          delay: 60